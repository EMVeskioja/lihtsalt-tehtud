# Lihtsalt Tehtud — Vanilla Web (Spring Boot + Thymeleaf)

Minimal, style-pure site:
- Vanilla HTML/CSS/JS (no Tailwind/Bootstrap)
- SVG checkmark logo (crisp in browser)
- Pages: /, /services, /pricing, /cases, /contact, /eesti-tegijad, /logo-preview

## Run
```bash
mvn spring-boot:run
```

Open:
- http://localhost:8080/
- http://localhost:8080/logo-preview
